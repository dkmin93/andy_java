package day08.modi.member.pac2;

import day08.modi.member.pac1.A;

public class C {
	
	A a = new A(1); //public이니까 당연히 접근가능
	A a2 = new A("홍길동"); // C는 A와 같은 패키지가 아니므로 접근 불가능
	A a3 = new A(true); //A는 private이므로 B에서는 접근 불가능

}
